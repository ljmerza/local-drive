# Django Settings
DJANGO_SETTINGS_MODULE=config.settings

# Allowed hosts (comma-separated)
ALLOWED_HOSTS=localhost,127.0.0.1

# Secrets File Location (contains OAuth credentials and tokens)
SECRETS_FILE=/app/.secrets.json

# Note: Google OAuth credentials are now stored in secrets.json
# See secrets.json.example for the file format
